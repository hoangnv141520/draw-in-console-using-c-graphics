# Legacy sources

This directory preserves the original WinBGIm renderer and Python conversion scripts unchanged for reference.

They are not part of the CMake build. Some files contain absolute paths and legacy dependencies; the new code must not copy those patterns.
